mlx-audio>=0.4.3,<0.5
# Core mlx: 0.31.2 segfaults the batched generation path; 0.32.0 verified fixed
# (2026-07-11). Re-verify batch_generate before raising the ceiling — it's the
# canary for mlx regressions.
mlx>=0.31,!=0.31.2,<0.33
gradio>=5.0,<7
soundfile>=0.13
numpy>=2.0
yt-dlp>=2024.1
pysrt>=1.1
transformers>=5.5.0,<6

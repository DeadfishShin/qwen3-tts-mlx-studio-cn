mlx-audio>=0.4.3,<0.5
# Pin core mlx: 0.31.2 segfaults the batched generation path. Do not bump past 0.31.1
# without re-verifying batch_generate (it's the canary for mlx regressions).
mlx>=0.31,<0.31.2
gradio>=5.0,<7
soundfile>=0.13
numpy>=2.0
yt-dlp>=2024.1
pysrt>=1.1
transformers>=5.5.0,<6
